# 技术设计文档

## 架构概览

Ming Capability Pack 是一个 DeepSeek Harness 插件，作为智能中间层帮助用户从 1594+ 插件中自动选择和编排最佳组合。

## 核心设计原则

1. **不重复造轮子** - 复用现有插件生态
2. **智能推荐** - 基于多维度评分算法
3. **自动化** - 从意图理解到执行验证全自动
4. **可追溯** - 完整的证据链

## 系统架构

```
┌─────────────────────────────────────────┐
│  用户层                                  │
│  自然语言输入                            │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  Ming Capability Pack                    │
│  ┌────────────────────────────────┐     │
│  │ IntentAnalyzer                 │     │
│  │ 意图分析引擎                    │     │
│  └────────────────────────────────┘     │
│              ↓                           │
│  ┌────────────────────────────────┐     │
│  │ PluginSelector ⭐核心          │     │
│  │ 智能插件选择器                  │     │
│  └────────────────────────────────┘     │
│              ↓                           │
│  ┌────────────────────────────────┐     │
│  │ PluginInstaller                │     │
│  │ 自动安装器                      │     │
│  └────────────────────────────────┘     │
│              ↓                           │
│  ┌────────────────────────────────┐     │
│  │ Orchestrator                   │     │
│  │ 智能编排器                      │     │
│  └────────────────────────────────┘     │
│              ↓                           │
│  ┌────────────────────────────────┐     │
│  │ EvidenceCollector              │     │
│  │ 证据收集器                      │     │
│  └────────────────────────────────┘     │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  DeepSeek Harness 插件生态               │
│  1594+ 插件                              │
└─────────────────────────────────────────┘
```

## 核心算法：插件选择

### 评分维度

插件评分基于 4 个维度（权重总计 100%）：

1. **受欢迎程度 (40%)**
   - 星标数：`log10(stars + 1) * 10`
   - 安装次数：`log10(installCount + 1) * 5`
   - 增长速度：`log10(growth24h + 1) * 2`

2. **活跃度 (20%)**
   - 最近更新时间：`max(0, 20 - daysSince / 7)`
   - 越近更新分数越高

3. **相关性 (25%)**
   - 关键词匹配：每匹配一个 +10 分
   - 反向模式惩罚：每匹配一个 -20 分
   - 范围：0-100

4. **兼容性 (15%)**
   - 同作者加分：+20
   - 类别冲突：-30
   - 范围：0-120

### 算法流程

```typescript
for each requirement:
  1. 搜索插件市场（多个关键词）
  2. 去重
  3. 过滤反向模式
  4. 计算每个候选的评分
  5. 排序
  6. 选择最高分
  
验证整体兼容性
```

## 数据流

```
用户输入
  ↓
LLM 分析
  ↓
Intent {
  goal: string
  type: IntentType
  requirements: Requirement[]
  confidence: number
}
  ↓
知识图谱增强
  ↓
插件市场 API 查询
  ↓
Plugin[] 候选列表
  ↓
多维度评分
  ↓
Map<capability, Plugin> 最佳组合
  ↓
执行安装
  ↓
编排执行
  ↓
EvidenceCard 证据链
```

## 扩展点

### 1. 添加新的意图类型

```typescript
// src/types.ts
export enum IntentType {
  // 添加新类型
  VIDEO_PROCESSING = 'video-processing'
}

// 添加知识图谱
INTENT_CAPABILITY_MAP[IntentType.VIDEO_PROCESSING] = {
  capabilities: [
    { name: 'video-editing', priority: 'critical' },
    // ...
  ],
  keywords: ['video', 'clip', 'edit'],
  antiPatterns: ['image', 'audio']
}
```

### 2. 优化评分算法

修改 `PluginSelector.calculateScore()` 方法，调整权重或添加新维度。

### 3. 添加备选方案

在 `Orchestrator.execute()` 中实现失败重试和备选插件切换。

## 性能考虑

- **缓存插件搜索结果** - 避免重复 API 调用
- **并行安装** - 多个插件可并行安装
- **增量更新知识图谱** - 定期更新，不每次重建

## 安全考虑

- **API 密钥管理** - 使用环境变量
- **命令注入防护** - 验证安装命令
- **沙箱执行** - 依赖 Harness 的沙箱机制

## 测试策略

1. **单元测试** - 每个服务独立测试
2. **集成测试** - 端到端场景测试
3. **真实场景测试** - 10+ 真实用户需求

## 未来优化

1. **机器学习优化** - 根据用户反馈调整评分权重
2. **插件兼容性数据库** - 积累已知的兼容/冲突组合
3. **智能降级** - 当首选插件失败时自动切换
4. **用户偏好学习** - 记住用户的选择偏好
